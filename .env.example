# TravelShield AI - Environment Variables Template
# ALWAYS store actual keys in .env.local (which is gitignored)

# Server-Side AI API Keys (Never expose to client code)
OPENAI_API_KEY=

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
